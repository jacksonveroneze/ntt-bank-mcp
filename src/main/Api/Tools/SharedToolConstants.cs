namespace NttBankMcp.Api.Tools;

internal static class SharedToolConstants
{
    public const string CustomerIdParamDesc =
        "The unique numeric identifier of the customer (must be greater than zero).";
    
    public const string AccountIdParamDesc =
        "The unique numeric identifier of the account (must be greater than zero).";
}
